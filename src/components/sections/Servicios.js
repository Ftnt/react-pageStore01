import React from "react";
import useFetch from "../../hooks/useFetch";

const Servicios = ({ peticion, title }) => {
  const data = useFetch(peticion);

  return <div></div>;
};

export default Servicios;
